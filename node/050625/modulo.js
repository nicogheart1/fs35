function somma(a, b) {
    console.log(`La somma è: ${a + b}`);
}

function sottrazione(a,b) {
    console.log(`La sottrazione è: ${a-b}`);
}

export default { somma, sottrazione };
// module.exports = somma;